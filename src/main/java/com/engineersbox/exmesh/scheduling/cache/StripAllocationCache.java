package com.engineersbox.exmesh.scheduling.cache;

/* Keeps previous strip allocations for given tasks and resources.
 * If the same execution pattern is requested, the previously calculated
 * strip allocation can be used to avoid recomputing it.
 */
public class StripAllocationCache {
}
